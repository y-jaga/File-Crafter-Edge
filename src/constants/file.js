const UNEXPECTED_FILE_TYPE = {
  code: "UNEXPECTED_FILE_TYPE",
  message:
    "File type not matches with folder type, only 'csv', 'img', 'pdf' or 'ppt' file types is allowed",
};

module.exports = { UNEXPECTED_FILE_TYPE };
